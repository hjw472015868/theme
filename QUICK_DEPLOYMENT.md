# 快速部署指南

## 🎯 推荐方案：发布到 npm

### 为什么推荐发布到 npm？

1. ✅ **一次发布，所有项目可用** - 不需要每个项目都配置
2. ✅ **版本管理清晰** - 可以指定版本，支持回滚
3. ✅ **标准方式** - 所有项目都可以通过 `pnpm add` 安装
4. ✅ **支持构建** - 构建后的包可以直接使用

---

## 🚀 快速发布步骤

### 1. 构建包

```bash
cd packages/theme-system
pnpm build
```

### 2. 登录 npm（如果未登录）

```bash
npm login
```

### 3. 发布包

```bash
# 方式一：使用脚本（推荐）
pnpm publish:npm

# 方式二：直接发布
npm publish --access public
```

### 4. 在其他项目中使用

```bash
cd /path/to/your/project
pnpm add @km-design/theme-system
```

---

## 📝 更新流程

### 1. 修改包代码

```bash
cd packages/theme-system
# 编辑文件...
```

### 2. 更新版本号

```bash
# 方式一：手动更新 package.json 中的 version
# 方式二：使用 npm version（会自动更新并创建 git tag）
npm version patch  # 1.0.0 -> 1.0.1
npm version minor  # 1.0.0 -> 1.1.0
npm version major  # 1.0.0 -> 2.0.0
```

### 3. 构建和发布

```bash
pnpm publish:npm
```

### 4. 在项目中使用新版本

```bash
cd /path/to/your/project
pnpm update @km-design/theme-system
# 或指定版本
pnpm add @km-design/theme-system@1.0.1
```

---

## 🔧 临时方案：使用相对路径

如果暂时不想发布到 npm，可以使用相对路径：

### 1. 在项目 package.json 中添加

```json
{
  "dependencies": {
    "@km-design/theme-system": "file:../../brainstorming-css-ux/km-artizen-ui/packages/theme-system"
  }
}
```

**注意**: 路径需要根据项目位置调整。

### 2. 安装依赖

```bash
pnpm install
```

### 3. 使用包

```typescript
import { NextThemeProvider, useTheme } from '@km-design/theme-system';
```

---

## ⚠️ 重要提示

### 发布前检查

1. ✅ 确保 `package.json` 中的信息正确
2. ✅ 确保 `files` 字段包含需要发布的文件
3. ✅ 确保构建成功（`dist/` 目录存在）
4. ✅ 确保版本号已更新

### 发布后

1. ✅ 验证包已发布：`npm view @km-design/theme-system`
2. ✅ 在测试项目中安装并验证
3. ✅ 更新文档说明版本

---

## 📚 相关文档

- [完整部署指南](./DEPLOYMENT_GUIDE.md) - 详细的部署方案
- [使用指南](./USAGE_GUIDE.md) - 如何在项目中使用
- [开发指南](./DEVELOPMENT_GUIDE.md) - 如何开发和更新

---

**推荐**: 发布到 npm，一次配置，所有项目可用！

