# 开发工作流程

> 说明主题组件的开发流程和不同项目的使用方式

## 📋 目录

- [项目结构](#项目结构)
- [开发流程](#开发流程)
- [使用方式](#使用方式)
- [发布流程](#发布流程)
- [常见问题](#常见问题)

---

## 🏗️ 项目结构

### 当前项目（km-artizen-ui）

这是一个 **monorepo** 项目，包含：

```
km-artizen-ui/
├── packages/
│   └── theme-system/          # 主题组件包（开发位置）
│       ├── src/               # 源代码
│       ├── presets/           # 预设主题
│       ├── docs/              # 文档
│       └── dist/              # 构建输出
├── apps/
│   └── km-artizen-ai-ui/      # UmiJS 项目（使用主题组件）
│       └── package.json        # 使用 workspace:* 协议
└── package.json               # 根 package.json
```

### 主题组件开发位置

**主题组件在 `packages/theme-system/` 中开发**，这是：

- ✅ 主题组件的源代码位置
- ✅ 所有主题配置和预设主题的位置
- ✅ 文档和开发指南的位置
- ✅ 构建和发布的位置

---

## 🔄 开发流程

### 1. 在本地开发

#### 开发步骤

1. **修改主题组件代码**

   ```bash
   cd packages/theme-system
   # 修改 src/ 中的代码
   # 修改 presets/ 中的主题配置
   ```

2. **构建包**

   ```bash
   cd packages/theme-system
   pnpm build
   ```

3. **在当前项目中测试**

   ```bash
   # 当前项目使用 workspace:* 协议，自动使用最新版本
   cd apps/km-artizen-ai-ui
   pnpm dev
   ```

#### 使用 workspace 协议

当前项目（`apps/km-artizen-ai-ui`）使用 `workspace:*` 协议：

```json
// apps/km-artizen-ai-ui/package.json
{
  "dependencies": {
    "@km-design/theme-system": "workspace:*"
  }
}
```

**优势**：

- ✅ 无需安装，直接使用本地开发版本
- ✅ 修改代码后重新构建即可，无需重新安装
- ✅ 开发效率高，实时测试

### 2. 发布到 GitHub

#### 发布步骤

1. **完成开发和测试**

   ```bash
   cd packages/theme-system
   pnpm build
   pnpm type-check
   ```

2. **提交代码**

   ```bash
   cd packages/theme-system
   git add .
   git commit -m "feat: 添加新功能"
   ```

3. **推送到 GitHub**

   ```bash
   # 推送到 GitHub 仓库
   git push origin main

   # 创建并推送标签（新版本）
   git tag v1.0.1
   git push origin v1.0.1
   ```

4. **其他项目更新**

   其他项目（非当前 monorepo）可以通过 GitHub 安装：

   ```bash
   pnpm add git+https://github.com/hjw472015868/theme.git#v1.0.1
   ```

---

## 📦 使用方式

### 方式一：当前项目（monorepo 内部）

**使用 workspace 协议**（推荐用于开发）

```json
// apps/km-artizen-ai-ui/package.json
{
  "dependencies": {
    "@km-design/theme-system": "workspace:*"
  }
}
```

**特点**：

- ✅ 直接使用本地开发版本
- ✅ 修改代码后重新构建即可
- ✅ 无需安装，自动链接
- ✅ 适合开发和测试

**使用场景**：

- 当前项目（`km-artizen-ai-ui`）开发时使用
- 主题组件开发时测试使用

### 方式二：其他项目（外部项目）

**使用 GitHub 协议**（推荐用于生产）

```json
// 其他项目的 package.json
{
  "dependencies": {
    "@km-design/theme-system": "git+https://github.com/hjw472015868/theme.git#v1.0.0"
  }
}
```

**特点**：

- ✅ 使用 GitHub 发布的稳定版本
- ✅ 版本控制清晰（通过 Git 标签）
- ✅ 适合生产环境
- ✅ 其他项目可以独立使用

**使用场景**：

- 其他独立项目（如 `km-flow-ui`）
- 生产环境部署
- 需要稳定版本的项目

---

## 🚀 发布流程

### 完整发布流程（详细步骤）

#### 1. 开发阶段

```bash
# 进入主题包目录
cd packages/theme-system

# 修改代码
# - 修改 src/ 中的组件代码
# - 修改 presets/ 中的主题配置
# - 添加新主题文件到 presets/ 目录
# - 修改文档等

# 如果添加了新主题，需要运行生成脚本（会自动在 build 前运行）
pnpm run generate:themes

# 构建包（必须！）
pnpm build

# 检查构建结果
ls -la dist/

# 在当前项目中测试
cd ../../apps/km-artizen-ai-ui
pnpm dev

# 验证功能
# - 检查主题切换是否正常
# - 检查样式是否正确应用
# - 检查类型定义是否正确
# - 检查控制台是否有错误
```

#### 2. 测试验证阶段

```bash
# 在当前项目中测试
cd apps/km-artizen-ai-ui
pnpm dev

# 全面测试
# ✅ 测试所有主题切换
# ✅ 测试主题持久化（刷新页面）
# ✅ 测试新添加的主题
# ✅ 测试样式是否正确
# ✅ 检查控制台错误
# ✅ 检查类型错误（如果有 TypeScript）
```

#### 3. 提交代码阶段

```bash
# 回到主题包目录
cd packages/theme-system

# 检查修改的文件
git status

# 查看修改内容
git diff

# 添加修改的文件
git add .

# 提交代码（使用清晰的提交信息）
git commit -m "feat: 添加新功能描述"
# 或
git commit -m "fix: 修复问题描述"
# 或
git commit -m "docs: 更新文档"

# 提交信息格式建议：
# - feat: 新功能
# - fix: 修复问题
# - docs: 文档更新
# - style: 代码格式调整
# - refactor: 代码重构
# - test: 测试相关
# - chore: 构建/工具相关
```

#### 4. 推送到 GitHub 阶段

```bash
# 确保在主题包目录
cd packages/theme-system

# 检查远程仓库
git remote -v

# 推送到 GitHub main 分支
git push origin main

# 如果推送失败，可能需要先拉取最新代码
git pull origin main

# 然后再推送
git push origin main
```

#### 5. 创建并推送标签（版本发布）

```bash
# 确保在主题包目录
cd packages/theme-system

# 查看当前版本
cat package.json | grep version

# 确定新版本号（遵循语义化版本）
# - 主版本号（1.0.0）：不兼容的 API 修改
# - 次版本号（0.1.0）：向下兼容的功能性新增
# - 修订号（0.0.1）：向下兼容的问题修正

# 创建标签（使用 v 前缀）
git tag v1.0.1

# 查看创建的标签
git tag -l

# 推送标签到 GitHub
git push origin v1.0.1

# 或者推送所有标签
git push origin --tags
```

#### 6. 其他项目更新阶段

```bash
# 在其他项目中使用新版本
cd /path/to/other/project

# 方式一：使用 main 分支（自动获取最新版本）⭐ 推荐开发环境
pnpm add git+https://github.com/hjw472015868/theme.git#main
# 或修改 package.json：
# "@km-design/theme-system": "git+https://github.com/hjw472015868/theme.git#main"

# 方式二：更新到指定版本（稳定版本，推荐生产环境）
pnpm add git+https://github.com/hjw472015868/theme.git#v1.0.1

# 方式三：更新到最新版本（如果使用 latest 标签）
pnpm update @km-design/theme-system

# 重新安装依赖（如果使用 main 分支，需要删除 lock 文件）
rm -rf node_modules pnpm-lock.yaml
pnpm install

# 重新构建项目
pnpm build
```

**版本选择建议**：
- 🏗️ **开发环境**：使用 `#main` 分支，自动获取最新代码
- 🚀 **生产环境**：使用固定版本标签（如 `#v1.0.1`），稳定可靠

---

### 📋 发布检查清单

在发布前，请确保完成以下检查：

#### ✅ 代码检查

- [ ] 代码已通过构建（`pnpm build` 成功）
- [ ] 没有 TypeScript 类型错误
- [ ] 没有 ESLint 错误（如果有配置）
- [ ] 代码已测试通过

#### ✅ 功能检查

- [ ] 所有主题切换正常
- [ ] 主题持久化正常（刷新页面保持主题）
- [ ] 新添加的主题可以正常使用
- [ ] 样式正确应用
- [ ] 控制台没有错误

#### ✅ 文件检查

- [ ] `dist/` 目录已生成
- [ ] `dist/` 目录包含所有必需文件
- [ ] `presets/` 目录包含所有主题文件
- [ ] `src/presets/` 目录已同步（如果有新主题）

#### ✅ Git 检查

- [ ] 所有修改已提交
- [ ] 提交信息清晰明确
- [ ] 没有未提交的文件

#### ✅ 版本检查

- [ ] 已确定新版本号
- [ ] 版本号符合语义化版本规范
- [ ] 已创建对应的 Git 标签

---

### 🔄 完整发布流程示例

#### 示例：添加新主题并发布

```bash
# 1. 进入主题包目录
cd packages/theme-system

# 2. 添加新主题文件
cp presets/default.json presets/my-new-theme.json
# 编辑 my-new-theme.json，修改主题配置

# 3. 生成导入代码（自动扫描并生成）
pnpm run generate:themes

# 4. 构建包
pnpm build

# 5. 测试（在当前项目中）
cd ../../apps/km-artizen-ai-ui
pnpm dev
# 在浏览器中测试新主题

# 6. 回到主题包目录
cd ../../packages/theme-system

# 7. 检查修改
git status
git diff

# 8. 提交代码
git add .
git commit -m "feat: 添加 my-new-theme 主题"

# 9. 推送到 GitHub
git push origin main

# 10. 创建并推送标签
git tag v1.0.1
git push origin v1.0.1

# 11. 在其他项目中使用
cd /path/to/other/project
pnpm add git+https://github.com/hjw472015868/theme.git#v1.0.1
```

#### 示例：修复问题并发布

```bash
# 1. 进入主题包目录
cd packages/theme-system

# 2. 修复问题
# 编辑 src/ 中的文件，修复 bug

# 3. 构建包
pnpm build

# 4. 测试
cd ../../apps/km-artizen-ai-ui
pnpm dev
# 验证问题已修复

# 5. 回到主题包目录
cd ../../packages/theme-system

# 6. 提交代码
git add .
git commit -m "fix: 修复主题持久化问题"

# 7. 推送到 GitHub
git push origin main

# 8. 创建并推送标签（修订版本）
git tag v1.0.2
git push origin v1.0.2

# 9. 在其他项目中更新
cd /path/to/other/project
pnpm add git+https://github.com/hjw472015868/theme.git#v1.0.2
```

---

### ⚠️ 重要注意事项

1. **必须构建后再推送**
   - ❌ 不要直接推送未构建的代码
   - ✅ 必须先运行 `pnpm build` 生成 `dist/` 目录
   - ✅ 确保 `dist/` 目录包含所有必需文件

2. **版本号管理**
   - 遵循语义化版本规范（Semantic Versioning）
   - 主版本号：不兼容的 API 修改
   - 次版本号：向下兼容的功能性新增
   - 修订号：向下兼容的问题修正

3. **标签命名**
   - 使用 `v` 前缀：`v1.0.1`
   - 与 `package.json` 中的版本号保持一致（可选）

4. **提交信息规范**
   - 使用清晰的提交信息
   - 遵循约定式提交规范（Conventional Commits）
   - 格式：`<type>: <description>`

5. **测试优先**
   - 发布前必须测试
   - 在当前项目中充分测试
   - 确保功能正常后再发布

---

## 🔧 开发最佳实践

### 1. 开发新功能

```bash
# 1. 在 packages/theme-system 中开发
cd packages/theme-system

# 2. 修改代码
# 编辑 src/ 中的文件

# 3. 构建
pnpm build

# 4. 在当前项目中测试
cd ../../apps/km-artizen-ai-ui
pnpm dev
```

### 2. 添加新主题

```bash
# 1. 创建主题文件
cd packages/theme-system
# 在 presets/ 中创建新主题 JSON 文件
# 例如：presets/my-new-theme.json

# 2. 确保主题文件包含所有必需字段
# - meta（元信息）
# - colors（颜色配置）
# - typography（字体配置，必需）
# - spacing（间距配置，必需）
# - borderRadius（圆角配置，必需）
# - borders（边框配置，必需）
# - shadows（阴影配置，必需）
# - animation（动画配置，必需）

# 3. 生成导入代码（自动扫描 presets/ 目录）
pnpm run generate:themes
# 这会自动更新 nextjs-adapter.tsx 和 index.ts

# 4. 构建包（必须！）
pnpm build

# 5. 测试
cd ../../apps/km-artizen-ai-ui
pnpm dev
# 在浏览器中测试新主题

# 6. 如果测试通过，提交并发布
cd ../../packages/theme-system
git add .
git commit -m "feat: 添加 my-new-theme 主题"
git push origin main
git tag v1.0.1
git push origin v1.0.1
```

### 3. 修复问题

```bash
# 1. 在 packages/theme-system 中修复
cd packages/theme-system

# 2. 修改代码
# 修复问题（编辑 src/ 中的文件）

# 3. 构建包（必须！）
pnpm build

# 4. 测试
cd ../../apps/km-artizen-ai-ui
pnpm dev
# 验证问题已修复

# 5. 如果测试通过，提交并发布
cd ../../packages/theme-system
git add .
git commit -m "fix: 修复问题描述"
git push origin main
git tag v1.0.2
git push origin v1.0.2
```

---

## ❓ 常见问题

### Q: 当前项目使用主题组件需要安装吗？

**A**: 不需要。当前项目使用 `workspace:*` 协议，会自动链接到本地开发版本。

```json
// apps/km-artizen-ai-ui/package.json
{
  "dependencies": {
    "@km-design/theme-system": "workspace:*"
  }
}
```

### Q: 修改主题组件后需要重新安装吗？

**A**: 不需要。只需要重新构建即可：

```bash
cd packages/theme-system
pnpm build
```

然后重新启动项目：

```bash
cd apps/km-artizen-ai-ui
pnpm dev
```

### Q: 什么时候需要推送到 GitHub？

**A**: 当需要：

- 其他项目使用新版本时
- 发布稳定版本时
- 需要版本控制时

### Q: 当前项目和其他项目的使用方式有什么区别？

**A**:

| 项目                             | 使用方式  | 协议              | 特点                   |
| -------------------------------- | --------- | ----------------- | ---------------------- |
| **当前项目**（km-artizen-ai-ui） | workspace | `workspace:*`     | 直接使用本地开发版本   |
| **其他项目**（km-flow-ui）       | GitHub    | `git+https://...` | 使用 GitHub 发布的版本 |

### Q: 如何确保其他项目使用最新版本？

**A**: 有几种方式，根据使用场景选择：

#### 方式一：使用 main 分支（自动获取最新版本）⭐ 推荐

**适用于**：开发环境、需要实时获取最新功能

```json
// package.json
{
  "dependencies": {
    "@km-design/theme-system": "git+https://github.com/hjw472015868/theme.git#main"
  }
}
```

**特点**：
- ✅ 自动获取 main 分支的最新代码
- ✅ 无需手动更新版本号
- ✅ 每次 `pnpm install` 都会拉取最新代码
- ⚠️ 可能包含未测试的代码（开发版本）

**更新方式**：
```bash
# 删除 node_modules 和 lock 文件，重新安装
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

#### 方式二：使用固定版本标签（稳定版本）

**适用于**：生产环境、需要版本控制

```json
// package.json
{
  "dependencies": {
    "@km-design/theme-system": "git+https://github.com/hjw472015868/theme.git#v1.0.1"
  }
}
```

**特点**：
- ✅ 版本固定，稳定可靠
- ✅ 适合生产环境
- ❌ 需要手动更新版本号

**更新方式**：
```bash
# 更新到新版本
pnpm add git+https://github.com/hjw472015868/theme.git#v1.0.2
```

#### 方式三：定期更新脚本（推荐生产环境）

创建一个更新脚本，定期检查并更新：

```bash
# scripts/update-theme.sh
#!/bin/bash
echo "检查主题组件最新版本..."
LATEST_TAG=$(git ls-remote --tags https://github.com/hjw472015868/theme.git | tail -1 | sed 's/.*\///')
echo "最新版本: $LATEST_TAG"
pnpm add git+https://github.com/hjw472015868/theme.git#$LATEST_TAG
```

**使用**：
```bash
chmod +x scripts/update-theme.sh
./scripts/update-theme.sh
```

#### 方式四：使用 pnpm 的更新命令

```bash
# 更新到最新版本（如果使用 main 分支）
pnpm update @km-design/theme-system

# 或者删除后重新安装
pnpm remove @km-design/theme-system
pnpm add git+https://github.com/hjw472015868/theme.git#main
```

---

### Q: 固定版本号后如何自动获取最新版本？

**A**: 如果已经使用了固定版本（如 `#v1.0.1`），有以下几种方式：

#### 方案 1：改为使用 main 分支（最简单）

```bash
# 修改 package.json
# 将：
"@km-design/theme-system": "git+https://github.com/hjw472015868/theme.git#v1.0.1"
# 改为：
"@km-design/theme-system": "git+https://github.com/hjw472015868/theme.git#main"

# 重新安装
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

#### 方案 2：定期手动更新版本号

```bash
# 查看最新标签
git ls-remote --tags https://github.com/hjw472015868/theme.git | tail -1

# 更新到最新版本
pnpm add git+https://github.com/hjw472015868/theme.git#v1.0.2
```

#### 方案 3：使用通配符（Git 协议不支持，但可以手动维护 latest 标签）

在主题包中创建 `latest` 标签，指向最新版本：

```bash
# 在主题包中
cd packages/theme-system
git tag -f latest v1.0.2  # 将 latest 标签指向最新版本
git push origin latest --force
```

然后在其他项目中使用：

```json
{
  "dependencies": {
    "@km-design/theme-system": "git+https://github.com/hjw472015868/theme.git#latest"
  }
}
```

---

### 📌 推荐方案对比

| 方案 | 适用场景 | 优点 | 缺点 |
|------|---------|------|------|
| **main 分支** | 开发环境 | 自动获取最新，无需手动更新 | 可能不稳定 |
| **固定版本** | 生产环境 | 稳定可靠，版本可控 | 需要手动更新 |
| **latest 标签** | 生产环境 | 自动获取最新稳定版 | 需要维护标签 |
| **更新脚本** | 自动化 | 可自动化更新流程 | 需要编写脚本 |

**建议**：
- 🏗️ **开发环境**：使用 `#main` 分支
- 🚀 **生产环境**：使用固定版本标签，定期手动更新

### Q: 开发时如何快速测试？

**A**:

1. **使用 workspace 协议**（当前项目）：
   - 修改代码 → 构建 → 测试
   - 无需安装，自动使用最新版本

2. **使用开发模式**：
   ```bash
   cd packages/theme-system
   pnpm dev  # 监听文件变化，自动构建
   ```

### Q: 如何管理版本？

**A**:

1. **开发版本**：使用 `workspace:*`（当前项目）
2. **稳定版本**：使用 Git 标签（其他项目）
   - `v1.0.0` - 主版本.次版本.修订版本
   - `v1.0.1` - 修复版本
   - `v1.1.0` - 新功能版本

---

## 📝 总结

### 开发流程

1. **在 `packages/theme-system` 中开发**
2. **构建包**：`pnpm build`
3. **在当前项目中测试**：使用 `workspace:*` 协议
4. **推送到 GitHub**：发布稳定版本
5. **其他项目更新**：使用 GitHub 协议安装

### 使用方式

- **当前项目**：使用 `workspace:*` 协议（开发模式）
- **其他项目**：使用 `git+https://...` 协议（生产模式）

---

---

## 📌 快速参考

### 常用命令

```bash
# 进入主题包目录
cd packages/theme-system

# 生成主题导入代码
pnpm run generate:themes

# 构建包
pnpm build

# 类型检查
pnpm type-check

# 开发模式（监听文件变化）
pnpm dev

# 查看 Git 状态
git status

# 查看修改内容
git diff

# 提交代码
git add .
git commit -m "feat: 描述"

# 推送到 GitHub
git push origin main

# 创建标签
git tag v1.0.1

# 推送标签
git push origin v1.0.1

# 查看所有标签
git tag -l

# 删除标签（本地）
git tag -d v1.0.1

# 删除标签（远程）
git push origin --delete v1.0.1
```

### 发布流程速查

```bash
# 1. 修改代码
# 2. 生成导入（如添加新主题）
pnpm run generate:themes

# 3. 构建
pnpm build

# 4. 测试
cd ../../apps/km-artizen-ai-ui && pnpm dev

# 5. 提交
cd ../../packages/theme-system
git add . && git commit -m "feat: 描述"

# 6. 推送
git push origin main

# 7. 打标签
git tag v1.0.1 && git push origin v1.0.1
```

---

**最后更新**: 2025-11-10  
**版本**: v1.0.0
